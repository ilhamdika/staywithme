    <div class="tab-pane fade show active" id="show-image" role="tabpanel" aria-labelledby="home-tab">
        <!-- DataTales Example -->
        <a href="/admin/item" class="btn btn-warning" style="margin-top: 20px;">back</a>
        <div class="row">
          <% for(let x = 0; x < item.imageId.length; x++){ %>
            <div class="col-md-4">
               <div class="card shadow mb-4 mt-2">
                 <div class="card-body">
                <img src="http://localhost:3000/<%= item.imageId[x].imageUrl %>" alt="image" style="width : 200px;">
                 </div>
              </div>
            <% } %>
            </div>
          </div>
    </div>